

EvalData = base
cls_Eval = EvalCls(EvalData)
ranking_Eval = EvalRank(EvalData)




class eva(object):
    def __init__(Data):
        self.DataObject = Data


    
    def compute(self):
        embedding = self.Data[Fields.embedding]